class Controler {
    constructor() { }
    update(dt) { }
    draw(drawer) { }

    initialize(lander) { }
    initializeFromJSON(lander, data) {
        this.initialize(lander);
    }

    stringify() { return { } }
}
